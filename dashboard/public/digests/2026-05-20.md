# 2026-05-20 — MultiversX Proof of Progress

**Window:** 2026-05-18 → 2026-05-20 (2-day digest)

## TL;DR

A short but dense window. The dominant event is a 78-commit mega-merge that pulls `rc/supernova` into a new intermediate branch, `feat/testnet-fixes`, and explicitly includes "multiple advisory fixes" folded alongside proof handling and backward compatibility work — the same quiet-advisory pattern as the Apr 23 v1.11.5.0 patch. Four smaller Supernova-calibration PRs (resolver dedup, cache cleanup, rounds-per-epoch, metachain transition check) also landed on that same branch. The most notable new signal is a draft GRPC host driver for the node (#7845), disabled by default but targeting `rc/supernova`. The Rust SDK's v0.66 RC picked up a wallet bugfix (its 253rd accumulated commit); `sc-meta source pack` and reproducible builds remain in draft.

## Since the last digest

**New:**
- `feat/testnet-fixes` integration branch created — 5 PRs merged into it in two days; it is now the active staging ground between `rc/supernova` and testnet
- [#7854](https://github.com/multiversx/mx-chain-go/pull/7854) 78-commit mega-merge of `rc/supernova` → `feat/testnet-fixes`, including advisory fixes (see Security below)
- [#7851](https://github.com/multiversx/mx-chain-go/pull/7851) metachain transition fix merged into `rc/supernova` directly
- [#7849](https://github.com/multiversx/mx-chain-go/pull/7849) rounds-per-epoch updated — **was open in last digest, now merged**
- [#7852](https://github.com/multiversx/mx-chain-go/pull/7852) hash deduplication on resolvers
- [#7825](https://github.com/multiversx/mx-chain-go/pull/7825) conditional cache cleanup in `UpdateContextForReplacedHeader`
- [mx-sdk-rs #2366](https://github.com/multiversx/mx-sdk-rs/pull/2366) wallet bugfix → `rc/v0.66`
- [#7845](https://github.com/multiversx/mx-chain-go/pull/7845) WIP GRPC host driver (new signal, draft)
- [#7847](https://github.com/multiversx/mx-chain-go/pull/7847) unbond period metrics (open, advancing)

**Still open / stalled from last digest:**
- `#7842` base timestamp — not yet merged
- `#7846` tx timestamp computation — still open
- `#7826` RWA integration tests — still open
- `#7841` outgoing txs inclusion check — still draft

**No new releases** in this window across any watched repo.

## Themes

### 1. `feat/testnet-fixes` — the new staging branch

The team has introduced `feat/testnet-fixes` as a dedicated integration branch between `rc/supernova` and the live testnet. All Supernova-calibration work is now landing here first, then flowing upstream. Draft PR [#7853](https://github.com/multiversx/mx-chain-go/pull/7853) batches 13 commits from this branch back up to `rc/supernova`; CI is currently failing, so the loop isn't closed yet.

### 2. Supernova testnet calibration (four PRs)

- [#7849](https://github.com/multiversx/mx-chain-go/pull/7849) — updated rounds-per-epoch in chain-simulator integration tests (miiu96)
- [#7852](https://github.com/multiversx/mx-chain-go/pull/7852) — hash deduplication on resolvers, 4 commits, full test coverage (miiu96)
- [#7825](https://github.com/multiversx/mx-chain-go/pull/7825) — cache cleanup now conditional on non-matching header hash in `UpdateContextForReplacedHeader`; prevents unnecessary cache invalidation (AdoAdoAdo)
- [#7851](https://github.com/multiversx/mx-chain-go/pull/7851) — moved a validation check to after-merge in the metachain epoch-transition path, targeted directly into `rc/supernova` (sstanculeanu)

### 3. Advisory signal — multiple fixes folded into the mega-merge

[#7854](https://github.com/multiversx/mx-chain-go/pull/7854) (78 commits, merged May 19) integrates `rc/supernova` into `feat/testnet-fixes`. Its commit log includes "security hardening: multiple advisory fixes and backward compatibility corrections" alongside contributors AdoAdoAdo, sstanculeanu, raduchis, sasurobert, and ssd04. The pattern — multiple advisory SHAs embedded in a large integration PR rather than standalone advisory PRs — is identical to the Apr 23 v1.11.5.0 coordinated patch. No public GHSA disclosures visible yet. **Watch for a follow-on mainnet-config tag.**

### 4. New signal: GRPC host driver

[#7845](https://github.com/multiversx/mx-chain-go/pull/7845) (WIP draft, miiu96) adds a gRPC interface to the blockchain node. The feature is disabled by default and coordinated with `mx-chain-es-indexer-go` PR #377 and `mx-chain-core-go` PR #434. This is the first concrete step toward a structured external communication layer for the node, potentially replacing/supplementing the existing HTTP REST and WebSocket patterns.

### 5. Supernova observability — unbond period metrics

[#7847](https://github.com/multiversx/mx-chain-go/pull/7847) (open, axenteoctavian) adds three new `StakingSystemSCConfig` metrics to `nodeRunner.go`, needed for integration tests on internal testnets. Targets `rc/supernova`. The PR was initially failing tests; now passing. Confirms active internal testnet work.

### 6. mx-sdk-rs v0.66 RC — wallet bugfix

[#2366](https://github.com/multiversx/mx-sdk-rs/pull/2366) (andrei-marinica, May 19) — wallet minor fix merged into `rc/v0.66`. The RC draft PR (#2332) now carries 253 accumulated commits. `sc-meta source pack` (#2354) and reproducible builds (#2355) remain in draft; no v0.66 tag yet.

## Version map

| Component | Stable | Pre-release | Δ |
|---|---|---|---|
| mx-chain-go | v1.11.5.0 (Apr 23) | v2.0.0 Supernova (Apr 2) | unchanged |
| mx-chain-vm-go | v1.5.45 (Apr 23) | — | unchanged |
| mx-sdk-rs | v0.65.1 (Mar 25) | rc/v0.66 (253 commits) | RC ticking |
| mx-sdk-js-core | v15.4.0 (Feb 19) | — | unchanged |
| mx-sdk-dapp | v5.6.23 (Mar 27) | — | unchanged |
| mx-bridge-eth-go | v3.1.0 (Apr 6) | — | unchanged |

## Strategic read

1. **`feat/testnet-fixes` is the new release gate.** The team now has three layers: `rc/supernova` (integration), `feat/testnet-fixes` (testnet-hardening staging), and eventually a mainnet-config tag. The branch structure signals active internal testnet operation.
2. **Watch #7854's advisory commits.** The pattern (multiple advisory SHAs embedded in a large merge, same contributors as Apr 23) suggests another coordinated patch cycle may be building. A mainnet-config tag or GHSA disclosure would confirm it.
3. **GRPC driver is a structural bet.** If #7845 merges, it signals that the team wants a typed, versioned external protocol for node interaction — potentially significant for tooling ecosystem (indexers, oracles, bridges).
4. **Supernova testnet is live and being tuned.** Rounds-per-epoch changes, unbond metrics, and cache calibration all indicate an active internal testnet with real feedback loops, not just theoretical pre-release work.
5. **mx-sdk-rs v0.66 is close but not there.** 253 RC commits but `reproducible builds` and `sc-meta source pack` still in draft — those look like the blocking items before a tag.
6. **`mx-chain-vm-go` and `mx-api-service` quiet this cycle.** No merged PRs in the 2-day window. Not alarming at this cadence, but worth noting if silence extends another week.

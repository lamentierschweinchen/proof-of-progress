# 2026-09-17 — MultiversX Proof of Progress

**Window:** 2026-09-16 → 2026-09-17 (since last digest)

## TL;DR

Quiet on the merge front — only one PR closed since yesterday, `mx-sdk-rs` [#2424](https://github.com/multiversx/mx-sdk-rs/pull/2424) "ABI new Unmanaged trait" — but today's real story is a correction to this digest's own record. The last three digests claimed `rc/v0.67` had "taken no commits since Jun 22," based on inspecting PR [#2378](https://github.com/multiversx/mx-sdk-rs/pull/2378)'s commit list; that list silently truncates at 100 commits, and the actual `rc/v0.67` branch has 231 commits over master with its most recent merge on **Sep 3** (via [#2422](https://github.com/multiversx/mx-sdk-rs/pull/2422)). Worse, the "ABI/proxy refactor moving on master while RC stays frozen" framing was backwards on both counts: #2424 and its predecessor [#2415](https://github.com/multiversx/mx-sdk-rs/pull/2415) merge into `feat/abi-crate`, not master, and `feat/abi-crate` itself targets `rc/v0.67` — so this work is explicitly RC-bound, not a parallel master-track story. On `mx-chain-go`, two new PRs appeared: [#8017](https://github.com/multiversx/mx-chain-go/pull/8017) fixes API status reporting for failed cross-shard move-balance transactions but has two dismissed reviews and unresolved Copilot-flagged correctness gaps; [#8018](https://github.com/multiversx/mx-chain-go/pull/8018) is a 747-line storage-engine config change, empty PR body, zero reviews. `mx-chain-proxy-go` [#522](https://github.com/multiversx/mx-chain-proxy-go/pull/522) is unchanged for a sixth day. Stats: 406 commits / 59 PRs merged in the rolling 28-day window (2026-08-20 → 2026-09-17), `AdoAdoAdo` (Adrian Dobrita) top contributor at 93 commits.

## Since the last digest

- **Correction to prior digests: `rc/v0.67` is not frozen.** Digests on Sep 14, 15, and 16 stated the `rc/v0.67` release-candidate branch had taken no commits since Jun 22, "confirmed via full commit history" by reading `gh pr view 2378 --json commits`. That call silently caps at 100 commits (oldest-first) and PR #2378 has 231 commits total against its `master` base — the check was only ever looking at the first ~43% of the branch's history. The actual `rc/v0.67` git ref's tip is a Sep 3 14:15:13Z merge commit (["renamed tx.rs -> tx_cli.rs"](https://github.com/multiversx/mx-sdk-rs/pull/2422)), confirmed via `gh api repos/multiversx/mx-sdk-rs/commits?sha=rc/v0.67` and `compare/master...rc/v0.67` (231 ahead / 2 behind). Real gap: 14 days since the last merge into the branch (Sep 3 → Sep 17), not "since Jun 22."
- **The "ABI work on master, RC frozen" split was also backwards.** [#2415](https://github.com/multiversx/mx-sdk-rs/pull/2415) (merged Sep 15) and [#2424](https://github.com/multiversx/mx-sdk-rs/pull/2424) (merged today, 23:13:41Z Sep 16) both merge into `feat/abi-crate`, not `master`. `feat/abi-crate` itself ([#2302](https://github.com/multiversx/mx-sdk-rs/pull/2302), open since Mar 3, still draft) has `rc/v0.67` as its base branch — meaning this entire ABI/proxy refactor line is explicitly targeting the v0.67 release candidate, not running in parallel with it. #2302's `updatedAt` moved to 23:20:53Z Sep 16, seven minutes after #2424 merged into it.
- **New: `mx-sdk-rs` [#2424](https://github.com/multiversx/mx-sdk-rs/pull/2424) "ABI new Unmanaged trait"** merged 23:13:41Z, 529/134 lines across 93 files. Removes `TypeAbi::Unmanaged` in favor of a dedicated `HasUnmanaged` trait plus a new `UnmanagedApi` marker so managed types built on the scenario `StaticApi` can act as their own unmanaged form. Direct continuation of the #2415 refactor line; [#2419](https://github.com/multiversx/mx-sdk-rs/pull/2419) "Pure ABI traits/types/macros" remains the still-draft next chapter, untouched since Aug 27.
- **New: `mx-chain-go` [#8017](https://github.com/multiversx/mx-chain-go/pull/8017)** "Fix API status for failed move-balance txs," opened by `miiu96` Sep 16 16:07 UTC. Fixes cross-shard move-balance transactions that fail on the destination shard (e.g. username mismatch) but were reported `success` via the API. 574 lines across 4 files, not draft. Two human reviews (`ssd04`, `axenteoctavian`) were **dismissed** after a follow-up commit; GitHub Copilot's automated review then flagged two unresolved correctness gaps — the new status filter is applied after `putResultsInTransaction`'s early-return on `ReceiptsHash`, so a transfer that emits both a source-side refund receipt and a destination-side error SCR never gets the correction applied, and the filter itself lacks a destination-shard guard so a source-shard query could start reporting `fail` for the same tx a source block still shows as `success`. `mergeStateStatus: BLOCKED`.
- **New: `mx-chain-go` [#8018](https://github.com/multiversx/mx-chain-go/pull/8018)** "storage config update," opened by `AdoAdoAdo` Sep 16 17:37 UTC. Draft, 747/65 lines across 21 files — touches `storage/factory/{dbConfigHandler,persisterFactory,persisterCreator,storageServiceFactory}.go`, adds a new `engineResources.go` and a new `storageServiceFactoryPebble_test.go`, plus `go.mod`/`go.sum` bumps. Empty PR-template body. Zero reviews. This is a separate track from the staking/delegation incident hardening — storage-engine configuration, not delegation logic. `mx-chain-storage-go` (outside the watchlist) shows a non-default-branch push at 18:11 UTC same day, consistent with related work in flight there, but no PR yet on that repo.
- **`mx-chain-go` [#8016](https://github.com/multiversx/mx-chain-go/pull/8016)** "staking instrumentation for importdb" is unchanged — still draft, zero reviews, `updatedAt` frozen at Sep 15 14:20:54Z, now stalled about 45 hours, the same pattern flagged on #522.
- **`mx-chain-proxy-go` [#522](https://github.com/multiversx/mx-chain-proxy-go/pull/522) is unchanged for a sixth consecutive digest.** Still draft, zero reviews, `updatedAt` Sep 11 22:39:18Z — six days now, four days past the point the underlying chain fix (v2.0.9) reached mainnet config.
- **`mx-chain-mainnet-config` has still not cut a fix1-equivalent release**, more than two days after `mx-chain-go` [v2.0.9-fix1](https://github.com/multiversx/mx-chain-go/releases/tag/v2.0.9-fix1) tagged (Sep 15). Config repo sits at v2.0.9.0 (Sep 14). This is now a real gap, not the one-day "worth a watch" flagged yesterday — v2.0.9 itself promoted in 9 minutes; fix1 has waited over 48 hours with no config PR visible.
- **Org-wide sweep**: no new repos outside the watchlist show substantive engineering activity. `mx-explorer-dapp` merged a routine "Development" PR ([#238](https://github.com/multiversx/mx-explorer-dapp/pull/238)) today; `mx-chain-tools-go` and `multiversx-etl` show `pushed_at` timestamps today but no new default-branch merges (tools-go's default branch is still frozen at Dec 15, 2025 — non-default-branch pushes, not investigated further per routine). Nothing else worth flagging.
- **`sasurobert`**: still no new activity. Last public event remains the Sep 14 06:10:53Z PR review on `mx-chain-go`, now three days quiet.

## Themes

### Digest hygiene: a three-digest-running factual error, corrected

- The `rc/v0.67`-is-frozen-since-June narrative, repeated in the Sep 14, 15, and 16 digests as a "confirmed" fact, was an artifact of `gh pr view --json commits` truncating at 100 commits. The branch has been actively merged into as recently as Sep 3 ([#2422](https://github.com/multiversx/mx-sdk-rs/pull/2422)), and the "master keeps moving, RC is stuck" framing had the destination backwards: the active ABI/proxy work merges into `feat/abi-crate`, which targets `rc/v0.67` directly.
- Going forward, branch-freshness claims in this digest should be checked against `gh api repos/<repo>/commits?sha=<branch>` or `compare/<base>...<branch>` (which return `total_commits`/`ahead_by` without silent truncation), not a PR's own `commits` field.

### Post-incident hardening on staking/delegation: two loose ends now, not one

- [#522](https://github.com/multiversx/mx-chain-proxy-go/pull/522) (proxy-side circuit breaker) — six days old, zero reviews, now the longest-running open item in this digest's history.
- [#8016](https://github.com/multiversx/mx-chain-go/pull/8016) (delegation-rewards import instrumentation) — now also stalled, 45 hours with zero reviews, same shape as #522.
- Neither is emergency-critical (the underlying chain fix is live), but both are now old enough that "still in review queue" is a weaker explanation than "parked."

### `mx-sdk-rs` ABI/proxy refactor continues inside `feat/abi-crate`

- [#2424](https://github.com/multiversx/mx-sdk-rs/pull/2424) merged — replaces `TypeAbi::Unmanaged` with a `HasUnmanaged` trait and a new `UnmanagedApi` marker, 93 files touched.
- [#2419](https://github.com/multiversx/mx-sdk-rs/pull/2419) "Pure ABI traits/types/macros" remains the queued next step, draft since Aug 27.
- Correctly framed now: this is v0.67-bound work via `feat/abi-crate` → `rc/v0.67`, not a master-only initiative running alongside a stalled RC.

### New, unrelated `mx-chain-go` work: storage engine and API status correctness

- [#8018](https://github.com/multiversx/mx-chain-go/pull/8018) opens a storage-engine configuration change (persister/engine-resources refactor, possible Pebble DB groundwork given the new `storageServiceFactoryPebble_test.go`) — unrelated to the staking incident, draft, unreviewed.
- [#8017](https://github.com/multiversx/mx-chain-go/pull/8017) fixes a real correctness bug (failed cross-shard move-balance txs reporting `success`), but its own automated review found the fix is itself incomplete in two specific ways (receipt early-return bypass, missing destination-shard guard) — worth watching whether those get addressed before merge or ship as a known gap.

## Version map

| Component | Stable | Pre-release | Δ |
|---|---|---|---|
| mx-chain-go | [v2.0.8](https://github.com/multiversx/mx-chain-go/releases/tag/v2.0.8) (Sep 10) | [v2.0.9-fix1](https://github.com/multiversx/mx-chain-go/releases/tag/v2.0.9-fix1) (Sep 15) | unchanged; #8017/#8018 new, #8016 stalled |
| mx-chain-mainnet-config | [v2.0.9.0](https://github.com/multiversx/mx-chain-mainnet-config/releases/tag/v2.0.9.0) (Sep 14) | — | unchanged; now 2+ days behind fix1 with no visible config PR |
| mx-chain-testnet-config | T2.0.9.0 (Sep 14) | — | unchanged |
| mx-chain-devnet-config | — | D2.0.9.0 (Sep 14) | unchanged |
| mx-chain-vm-go | [v1.5.47](https://github.com/multiversx/mx-chain-vm-go/releases/tag/v1.5.47) (Jul 30) | [v1.5.49](https://github.com/multiversx/mx-chain-vm-go/releases/tag/v1.5.49) (Sep 10) | unchanged |
| mx-chain-proxy-go | — (all pre-release) | [v1.5.1](https://github.com/multiversx/mx-chain-proxy-go/releases/tag/v1.5.1) (Aug 19) | unchanged tag; #522 still draft/unreviewed, now 6 days old |
| mx-chain-simulator-go | [v1.11.8](https://github.com/multiversx/mx-chain-simulator-go/releases/tag/v1.11.8) (Jul 3) | — | unchanged |
| mx-chain-es-indexer-go | [v1.10.4](https://github.com/multiversx/mx-chain-es-indexer-go/releases/tag/v1.10.4) (Sep 8) | — | unchanged |
| mx-sdk-rs | [v0.66.2](https://github.com/multiversx/mx-sdk-rs/releases/tag/v0.66.2) (Jun 18) | rc/v0.67 (branch tip Sep 3, corrected from prior digests' "Jun 22") | unchanged tag; #2424 merged into feat/abi-crate → rc/v0.67 |
| mx-sdk-dapp | [v5.7.3](https://github.com/multiversx/mx-sdk-dapp/releases/tag/v5.7.3) (Sep 14) | — | unchanged |
| mx-sdk-js-core | [v16.0.0](https://github.com/multiversx/mx-sdk-js-core/releases/tag/v16.0.0) (Jul 27) | — | unchanged |
| mx-template-dapp | — (unversioned repo) | — | unchanged |
| mx-api-service | [v1.21.0](https://github.com/multiversx/mx-api-service/releases/tag/v1.21.0) (Aug 19) | — | unchanged |
| mx-bridge-eth-go | [SUI with Mint-Burn / v3.1.0](https://github.com/multiversx/mx-bridge-eth-go/releases/tag/v3.1.0) (Apr) | [v3.0.22](https://github.com/multiversx/mx-bridge-eth-go/releases/tag/v3.0.22) (Sep 2) | unchanged |

## Strategic read

1. **This digest's own methodology needed a fix today, and that's worth taking seriously.** A truncated API call produced a "confirmed" claim that survived three consecutive digests and shaped the strategic reads in all of them ("framework alive, RC frozen"). The actual situation — ABI/proxy work explicitly targets the RC branch — is a more coherent story than the one previously told. Branch-history checks from here on should use `compare`/`commits` endpoints, which report totals directly, rather than a PR's own commit list.
2. **The proxy-side mitigation ([#522](https://github.com/multiversx/mx-chain-proxy-go/pull/522)) is now the oldest unresolved item this digest has tracked** — six days, zero reviews. A second item ([#8016](https://github.com/multiversx/mx-chain-go/pull/8016)) has joined it in the same "opened, then untouched" state. Two parked drafts on the same incident's follow-through is worth a direct ping to whoever owns proxy-go and the delegation-instrumentation work, rather than a seventh consecutive flag here.
3. **`mx-chain-mainnet-config` lagging `v2.0.9-fix1` by 48+ hours has crossed from "one-day watch" to "actual gap"**, especially set against v2.0.9's 9-minute turnaround on Sep 14. Worth confirming whether fix1 is intentionally being held back (e.g. bundled into the next full config release) rather than assuming an oversight.
4. **New `mx-chain-go` activity ([#8017](https://github.com/multiversx/mx-chain-go/pull/8017), [#8018](https://github.com/multiversx/mx-chain-go/pull/8018)) is broadening beyond the staking incident** — a storage-engine config change and an API-correctness fix, both unrelated to delegation. Normal engineering cadence resuming alongside the incident cleanup, not blocked by it.
5. **Automated review (GitHub Copilot) caught real, specific correctness gaps on #8017** before any human re-review — worth tracking whether the team treats that as sufficient signal to hold the merge or ships with the gaps noted as known limitations.
6. **`sasurobert` remains quiet for a third day.** No signal either way; noting only to preserve the digest's own tracking continuity.

# 2026-08-08 — MultiversX Proof of Progress

**Window:** 2026-08-07 → 2026-08-08 (since last digest)

## TL;DR

A third config knob just joined the same-day mx-chain-go/mainnet-config mirroring pattern flagged twice before — but this time it closed the loop cleanly. `SnapshotsGoroutinesPerCore` was bumped 5x (2→10) on [mx-chain-go #7949](https://github.com/multiversx/mx-chain-go/pull/7949) ("update snapshot routines per core," `AdoAdoAdo`, 08-07T12:16:44Z) and the identical change landed on [mx-chain-mainnet-config #259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259) less than two hours later ("updated SnapshotsGoroutinesPerCore," `sstanculeanu`, 08-07T14:04:59Z) — same file section, same old/new values. That's in contrast to yesterday's `MaxTxNonceDeltaAllowed`/`SyncProcessTimeSupernovaInMillis` bumps on #7951, which mainnet-config's #259 still has **not** picked up (confirmed directly against the live `config.toml` on the `release-v2.0.0.0` branch: still `100`/`6000`, not `1000`/`12000`). #7949 also picked up a small "fixes after review" commit touching trie/metrics files, bringing it to 31 commits, +6,195/−675. Everything else in the watchlist — #7951 itself, #2416, #7944/#7940/#7853, the mx-sdk-rs backlog, #518, #1597 — is static since yesterday; zero merges to any tracked default branch, zero new releases, sasurobert and both known advisory branches (`update-arwen-hotfix-master`, `hotfix-nil-worldmock` — confirmed last touched in 2021) remain quiet. Stats: 358 commits / 62 PRs merged in the rolling 28-day window (up from 354 commits; PR count keeps sliding down as the rolling window ages past late July, not a real slowdown — zero PRs merged today).

## Since the last digest

- [mx-chain-go #7949](https://github.com/multiversx/mx-chain-go/pull/7949) "fixes stall on snapshot" — two new commits from `AdoAdoAdo`: "update snapshot routines per core" (08-07T12:16:44Z) drops `SnapshotsGoroutinesPerCore` from `2` to `10` in `cmd/node/config/config.toml` (comment above the value: "scales the snapshot fan-out with the host: effective goroutines = value * GOMAXPROCS, minimum 4"), and "fixes after review" (08-07T14:07:56Z) touches `node/metrics/metrics.go`, `trie/errors.go`, `trie/trieStorageManager.go` and their test files (1 line each). Now 31 commits, +6,195/−675 (was 29 / +6,193/−675 as of yesterday's digest).
- [mx-chain-mainnet-config #259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259) "Release v2.0.0.0" — one new commit, "updated SnapshotsGoroutinesPerCore" (`sstanculeanu`, 08-07T14:04:59Z), applying the exact same `2`→`10` change to `config.toml`, ~2 hours after #7949's equivalent commit. This is a clean instance of the mirroring pattern the last two digests have been tracking — but it's selective: pulling the live `config.toml` off the `release-v2.0.0.0` branch directly confirms `MaxTxNonceDeltaAllowed` is still `100` (not #7951's `1000`) and `SyncProcessTimeSupernovaInMillis` is still `6000` (not `12000`). mainnet-config synced the snapshot-goroutines knob same-day but is still one PR-generation behind on the nonce-delta/sync-time knobs from #7951.
- [mx-chain-go #7951](https://github.com/multiversx/mx-chain-go/pull/7951) "extra txs api logs" — no new commits since yesterday's "update tx nonce max txpool + gas" (already covered).
- [mx-sdk-rs #2416](https://github.com/multiversx/mx-sdk-rs/pull/2416) "Relayed v3 support in sc-meta CLI & interactors" — no new commits since yesterday's "sc-meta CLI relayer and signing refactor" (already covered). Still +1,033/−87.
- [mx-chain-go #7944](https://github.com/multiversx/mx-chain-go/pull/7944), [#7940](https://github.com/multiversx/mx-chain-go/pull/7940), [#7853](https://github.com/multiversx/mx-chain-go/pull/7853) — all unchanged (last activity Aug 4 / Jul 30).
- mx-sdk-rs backlog unchanged: [#2415](https://github.com/multiversx/mx-sdk-rs/pull/2415) (16 days open), [#2404](https://github.com/multiversx/mx-sdk-rs/pull/2404) (50 days open), [#2374](https://github.com/multiversx/mx-sdk-rs/pull/2374) CVE-2025-64170 rpassword bump (81 days open) — none moved.
- [mx-chain-proxy-go #518](https://github.com/multiversx/mx-chain-proxy-go/pull/518) and [mx-api-service #1597](https://github.com/multiversx/mx-api-service/pull/1597) — both unchanged.
- No repo in the watchlist merged anything to its default branch or cut a new release in the window.

**GHSA-xr93-fx88-vm63 / GHSA-hwg4-v7gq-phfc:** no new signal. `sasurobert` shows no new merged PRs and no new public GitHub events since Jul 13; a direct commit search across mx-chain-go and mx-chain-vm-go shows his most recent commits predate the current 28-day window (last vm-go commit "hardening as always," 2026-07-07). The two advisory-named branches flagged previously — `update-arwen-hotfix-master` (mx-chain-go) and `hotfix-nil-worldmock` (mx-chain-vm-go) — were checked directly this run: both last committed to in **2021**, confirming they're long-dead branch names, not live advisory work.

**Org-wide sweep:** [mx-docs](https://github.com/multiversx/mx-docs) is the most recently pushed repo org-wide (2026-08-07T21:07:37Z) but its default branch's newest commit is still July 10 — the push landed on a non-default branch, same "push without merge" pattern seen repeatedly on other repos. `mx-chain-devnet-config` and `mx-chain-testnet-config` pushed within the same two-hour window as mainnet-config's `SnapshotsGoroutinesPerCore` commit (14:03–14:05Z) but both default branches are still frozen on their July 30 release merges — config-repo resync noise, not new engineering. No repo outside the existing watchlist/known-noise set showed substantive new activity.

## Themes

### A third config knob joins the mirroring pattern — and this one actually synced

Two digests running, `MaxTxNonceDeltaAllowed` has been the example of same-day (or next-day) tuning that moves in mx-chain-go before mainnet-config catches up, if it catches up at all. Today a different knob — `SnapshotsGoroutinesPerCore`, governing snapshot-goroutine fan-out per CPU core — went through the identical dance: introduced/changed on #7949, mirrored on #259 about two hours later, same file region, same before/after values (`2`→`10`). Unlike the nonce-delta knob, this one closed the loop same-day. Read together, this looks like a live, iterative tuning process on the stall-fixes/snapshot branch with mainnet-config picking values up in a rolling, sometimes-lagged fashion — worth treating each individual value as provisional until #259 merges, and worth watching whether the still-outstanding `MaxTxNonceDeltaAllowed`/`SyncProcessTimeSupernovaInMillis` gap closes the same way.

### #7949 keeps growing, five days running

Now 31 commits (was 29 yesterday, 28 the day before), +6,195/−675. Today's additions were incremental: a config value tune and a small review-feedback commit touching trie/metrics code. No scope change — this remains late-stage refinement on the epoch-transition/snapshot/sync path first flagged five digests ago.

### A quiet day otherwise

Zero merges to any default branch across the 14-repo watchlist, zero new releases, #7951 and #2416 both static since yesterday, and no new engineer or advisory signal. The rolling 28-day PR count (68→62) continues its expected decline as late-July's busier stretch ages out of the window; commits actually rose (354→358) on the back of today's #7949 activity.

## Version map

| Component | Stable | Pre-release | Δ |
|---|---|---|---|
| mx-chain-go | [v1.11.10](https://github.com/multiversx/mx-chain-go/releases/tag/v1.11.10) (Jul 30) | — | unchanged; #7949 +2 commits (SnapshotsGoroutinesPerCore 2→10, review fixes) |
| mx-chain-mainnet-config | [v1.11.10.0](https://github.com/multiversx/mx-chain-mainnet-config/releases/tag/v1.11.10.0) (Jul 30) | v2.0.0.0 ([#259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259)) | unchanged; #259 +1 commit, synced on snapshot-goroutines but still behind on nonce-delta/sync-time |
| mx-chain-vm-go | [v1.5.47](https://github.com/multiversx/mx-chain-vm-go/releases/tag/v1.5.47) (Jul 30) | — | unchanged; quiet |
| mx-chain-proxy-go | [v1.3.4](https://github.com/multiversx/mx-chain-proxy-go/releases/tag/v1.3.4) (Oct 29) | v1.4.0 (Mar 5) | unchanged; #518 no movement |
| mx-chain-simulator-go | [v1.11.8](https://github.com/multiversx/mx-chain-simulator-go/releases/tag/v1.11.8) (Jul 3) | — | unchanged |
| mx-sdk-rs | [v0.66.2](https://github.com/multiversx/mx-sdk-rs/releases/tag/v0.66.2) (Jun 18) | rc/v0.67 ([#2378](https://github.com/multiversx/mx-sdk-rs/pull/2378), 75 days open) | unchanged tag; #2416 static since yesterday |
| mx-sdk-dapp | [v5.7.1](https://github.com/multiversx/mx-sdk-dapp/releases/tag/v5.7.1) (Jul 14) | — | unchanged; quiet |
| mx-sdk-js-core | [v16.0.0](https://github.com/multiversx/mx-sdk-js-core/releases/tag/v16.0.0) (Jul 27) | — | unchanged |
| mx-template-dapp | — (unversioned repo) | — | quiet |
| mx-api-service | [v1.20.0](https://github.com/multiversx/mx-api-service/releases/tag/v1.20.0) (Jun 4) | — | unchanged; #1597 quiet |
| mx-chain-es-indexer-go | [v1.10.2](https://github.com/multiversx/mx-chain-es-indexer-go/releases/tag/v1.10.2) (Apr 3) | [v1.9.3-fix2](https://github.com/multiversx/mx-chain-es-indexer-go/releases/tag/v1.9.3-fix2) (Jul 22) | unchanged |

## Strategic read

1. **The mx-chain-go ↔ mainnet-config mirroring pattern is real and recurring, not a one-off.** Three knobs now — `MaxTxNonceDeltaAllowed`, `SyncProcessTimeSupernovaInMillis`, `SnapshotsGoroutinesPerCore` — have moved on the stall-fixes/snapshot work and been picked up (fully or partially) on #259 within hours to a day. This is the clearest window into how the Supernova release-config branch actually gets tuned: values move on the working PR first, mainnet-config catches up piecemeal.
2. **The catch-up is uneven, and that's the thing to watch.** `SnapshotsGoroutinesPerCore` synced same-day; `MaxTxNonceDeltaAllowed`/`SyncProcessTimeSupernovaInMillis` are now two days stale on #259. If #259 merges before those two catch up, mainnet-config would ship with the older values — worth flagging if it happens.
3. **#7949 is now five days and 31 commits deep** on a single author's (`AdoAdoAdo`) branch, still incremental in nature. No signal yet on a merge date, but the pace suggests it's converging rather than expanding.
4. **mx-sdk-rs's `v0.67` RC has gone quiet** — #2416 picked up nothing today after several straight days of single-commit progress; worth checking next digest whether that's a pause or the branch is between review rounds.
5. **Confirmed dead ends worth retiring from active tracking:** both previously-flagged "advisory" branch names (`update-arwen-hotfix-master`, `hotfix-nil-worldmock`) are verified stale since 2021 — they were naming coincidences, not live security work. GHSA-xr93-fx88-vm63 and GHSA-hwg4-v7gq-phfc remain genuinely undisclosed with no fresh signal from `sasurobert` or elsewhere.

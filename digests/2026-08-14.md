# 2026-08-14 — MultiversX Proof of Progress

**Window:** 2026-08-13 → 2026-08-14 (since last digest)

## TL;DR

After yesterday's first fully-quiet merge window, `feat/recon-by-meta` shipped: [mx-chain-go #7940](https://github.com/multiversx/mx-chain-go/pull/7940) merged the entire feature branch (22,385 insertions / 2,478 deletions across a month of work) into `feat/testnet-fixes`, `AdoAdoAdo`, 2026-08-14T13:46:12Z — closing out a branch this digest has tracked as "in progress" for over two weeks. Two smaller PRs landed on the branch just ahead of the merge: a bandwidth-usage improvement ([#7960](https://github.com/multiversx/mx-chain-go/pull/7960)) and a version bump pulling in a real WebSocket-reconnection fix from `mx-chain-communication-go` ([#7958](https://github.com/multiversx/mx-chain-go/pull/7958), fix itself at [communication-go #85](https://github.com/multiversx/mx-chain-communication-go/pull/85)). Separately, `mx-sdk-rs` finally merged its long-tracked Relayed v3 CLI/interactor support ([#2416](https://github.com/multiversx/mx-sdk-rs/pull/2416)) into `rc/v0.67`. The nonce-delta PR ([#7951](https://github.com/multiversx/mx-chain-go/pull/7951)) is still open and unmerged — it got swept along in the branch consolidation (retargeted from `feat/recon-by-meta` to `feat/testnet-fixes`) but its own content hasn't changed since Aug 10, so the underlying config values (`MaxTxNonceDeltaAllowed`, `SyncProcessTimeSupernovaInMillis`) are still not live on the target branch. No new release tags yet despite the merge. Stats: 332 commits / 56 PRs merged in the rolling 28-day window (2026-07-17 → 2026-08-14), `AdoAdoAdo` still the top contributor (125 commits).

## Since the last digest

- **[mx-chain-go #7940](https://github.com/multiversx/mx-chain-go/pull/7940) "feat/recon-by-meta" merged into `feat/testnet-fixes`** — 2026-08-14T13:46:12Z, `AdoAdoAdo`, +22,385/−2,478. This is the branch this digest has tracked since it opened ([#7925](https://github.com/multiversx/mx-chain-go/pull/7925) "Recon by meta", opened 2026-07-29), covering multi-proof pool/resolver work, cross-header settlement, and the various fixes/linter passes reported in prior digests. It does **not** land on `master` — `feat/testnet-fixes` is itself a staging branch, so this is a consolidation step, not a release. No new tag was cut alongside it.
- **[mx-chain-go #7960](https://github.com/multiversx/mx-chain-go/pull/7960) "Improve bandwidth usage" merged** — 2026-08-14T09:06:55Z, `AdoAdoAdo`, +328/−100, into `feat/recon-by-meta` shortly before that branch itself merged upward.
- **[mx-chain-go #7958](https://github.com/multiversx/mx-chain-go/pull/7958) "new mx-chain-communication go" merged** — 2026-08-13T13:22:30Z, `miiu96`, a `go.mod`/`go.sum` bump pulling in [mx-chain-communication-go #85](https://github.com/multiversx/mx-chain-communication-go/pull/85) "Fix WebSocket reconnection not triggering after broken pipe" (merged 2026-08-13T13:18:47Z) — the fix reported yesterday as still-open is now shipped. A follow-up, [communication-go #86](https://github.com/multiversx/mx-chain-communication-go/pull/86) "ignored messages in PubsubCallback" (`AdoAdoAdo`), merged 2026-08-14T09:06:45Z — not yet pulled into `mx-chain-go`.
- **[#7951](https://github.com/multiversx/mx-chain-go/pull/7951) "extra txs api logs" — retargeted, not resolved.** Its base branch was `feat/recon-by-meta`; now that branch is gone (merged into `feat/testnet-fixes`), GitHub auto-retargeted the PR, bumping `updatedAt` to 2026-08-14T13:46:15Z — coincident with the `#7940` merge, not a new commit. Still 6 commits, last real commit 2026-08-10T07:41:40Z, still open. Diffed directly against the new base: it still proposes `MaxTxNonceDeltaAllowed` 100→1000 and `SyncProcessTimeSupernovaInMillis` 6000→12000, confirmed unmerged (`feat/testnet-fixes`'s live `config.toml` still reads 100/6000). New detail not previously reported: the same PR also bumps `economics.toml`'s Supernova-paired gas entry (`EnableEpoch = 2`) — `MaxGasLimitPerBlock` from `600000000` to `6000000000`, a 10x increase — plus new `log.Trace` calls on failed tx create/validate in the multi-tx-submit API endpoint (explaining the PR's otherwise-cryptic title). This is now the **eighth straight digest** with no resolution.
- **[mx-sdk-rs #2416](https://github.com/multiversx/mx-sdk-rs/pull/2416) "Relayed v3 support in sc-meta CLI & interactors" merged** — 2026-08-13T21:16:15Z, `andrei-marinica`, +915/−220, into `rc/v0.67`. Tracked as the most active open PR in the repo across the last several digests; now closed out. A same-day follow-up opened immediately after: [#2418](https://github.com/multiversx/mx-sdk-rs/pull/2418) "Interactor refactor: deduplicating tx dispatch, chain simulator handling in sc-meta CLI" (same author, 2026-08-13T22:34:09Z).
- **[mx-chain-notifier-go #150](https://github.com/multiversx/mx-chain-notifier-go/pull/150) "api + dispatcher improvements" merged** — 2026-08-14T07:00:08Z, `ssd04`. Second consecutive-day merge from `ssd04` in this repo, following yesterday's [#149](https://github.com/multiversx/mx-chain-notifier-go/pull/149) (deterministic exec-result ordering). `ssd04` is the same author behind the still-open `#7951` — the pattern flagged yesterday (execution-ordering/nonce-adjacent hardening spanning `mx-chain-go` and `mx-chain-notifier-go`) continues, now with two days of notifier-go activity back to back.
- **[mainnet-config #259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259) "Release v2.0.0.0" — still no movement, third straight digest static.** Still 44 commits, still last updated 2026-08-11T11:41:22Z.
- **No new releases** across the watchlist. `mx-chain-go` stable/pre-release still [v1.11.11](https://github.com/multiversx/mx-chain-go/releases/tag/v1.11.11)/[v2.0.3](https://github.com/multiversx/mx-chain-go/releases/tag/v2.0.3), both tagged 2026-08-10 — notable that today's large branch merge did not trigger a new tag.
- **`sasurobert`: no new activity.** Search across the account's merged PRs and public events surfaces nothing since Jan/Feb 2026 personal-repo work (unrelated `sasurobert/x402`, `sasurobert/multiversx-mcp-server`); still nothing new in any watchlist repo since the already-covered `GHSA-xr93-fx88-vm63` fix (Jul 20–23, covered in the 2026-07-31 digest).
- **Org-wide sweep:** new open PR [mx-chain-go #7961](https://github.com/multiversx/mx-chain-go/pull/7961) "avoid root hash overwrite" (`BeniaminDrasovean`, opened 2026-08-14T10:25:52Z, targets `feat/testnet-fixes`) — no review activity yet, mentioned for continuity. Outside the watchlist, `mx-assets`, `mx-sdk-dapp-swap`, `mx-sdk-dapp-form`, `mx-sdk-js-web-wallet-io`, and `mx-explorer-dapp` all show pushes today but none surfaced via a watchlist-repo cross-reference (unlike `mx-chain-communication-go`/`mx-chain-notifier-go`), so not investigated further — flagging only in case a pattern emerges.
- mx-sdk-rs backlog otherwise unchanged: [#2415](https://github.com/multiversx/mx-sdk-rs/pull/2415), [#2404](https://github.com/multiversx/mx-sdk-rs/pull/2404), [#2374](https://github.com/multiversx/mx-sdk-rs/pull/2374) CVE-2025-64170 rpassword bump, and [rc/v0.67](https://github.com/multiversx/mx-sdk-rs/pull/2378) itself (100 commits, still open, now 81 days).
- [mx-api-service #1597](https://github.com/multiversx/mx-api-service/pull/1597) — unchanged since Jul 29.

## Themes

### `feat/recon-by-meta` ships after a month of tracked work

The branch this digest has followed since [#7925](https://github.com/multiversx/mx-chain-go/pull/7925) opened on Jul 29 finally merged upstream into `feat/testnet-fixes` via `#7940` — 22K+ lines, spanning multi-proof pool/resolver logic, cross-header settlement, contention handling for meta headers, snapshot-stall fixes, and the linter/bandwidth passes from the last two digests. It's a consolidation, not a release: `feat/testnet-fixes` is itself pre-release staging, and no tag was cut. Worth watching whether a `v2.0.4`/`v2.1.0`-class tag follows in the next few days now that this branch is closed out.

### The host-driver reliability fix from yesterday shipped, and picked up a companion

`mx-chain-communication-go #85` (the WebSocket-reconnect zombie-conn fix flagged yesterday as open) merged and was pulled into `mx-chain-go` same-day via `#7958`. A second `mx-chain-communication-go` PR (`#86`, ignored-messages handling in `PubsubCallback`) merged this morning but hasn't been pulled into `mx-chain-go` yet — worth checking next digest whether it lands alongside whatever follows `#7940`.

### `#7951` is retargeted but still not resolved — and reveals a previously-hidden gas-limit change

Yesterday's digest could only check the target branch's live config, which still read the old values. Diffing the PR directly today (now that its base moved to `feat/testnet-fixes`) confirms it still carries the proposed `MaxTxNonceDeltaAllowed`/`SyncProcessTimeSupernovaInMillis` bumps from Aug 7–10, unmerged. It also carries a 10x `MaxGasLimitPerBlock` increase for the Supernova-paired epoch in `economics.toml` that no prior digest reported — this diff-level detail was missed until directly diffing the PR rather than just the target branch's `config.toml`. Eight digests of no merge is now well past the point where restating "unchanged" is informative on its own.

### `ssd04`'s notifier-go / nonce-ordering thread continues for a second day

Following yesterday's flagged pattern, `ssd04` merged a second `mx-chain-notifier-go` PR today (`#150`, api + dispatcher improvements) right after yesterday's `#149` (deterministic exec-result ordering). Combined with `#7951` (also `ssd04`) sitting unmerged in `mx-chain-go`, this reads increasingly like a coordinated push on execution-ordering/nonce handling around the Supernova async-execution model — still not stated explicitly in any PR body, so treated as a pattern worth watching rather than a confirmed link.

### `mx-sdk-rs` clears its longest-tracked open PR, immediately opens the next one

`#2416` (Relayed v3 support in sc-meta CLI & interactors), tracked as the most active open PR in the repo for several digests running, merged into `rc/v0.67`. `andrei-marinica` opened a direct follow-up (`#2418`, interactor refactor) within the hour — the release candidate itself (`#2378`) is still open at 100 commits and 81 days, unaffected by this merge.

## Version map

| Component | Stable | Pre-release | Δ |
|---|---|---|---|
| mx-chain-go | [v1.11.11](https://github.com/multiversx/mx-chain-go/releases/tag/v1.11.11) (Aug 10) | [v2.0.3](https://github.com/multiversx/mx-chain-go/releases/tag/v2.0.3) (Aug 10) | unchanged tag; `feat/recon-by-meta` merged into `feat/testnet-fixes` (#7940), no new tag yet |
| mx-chain-mainnet-config | [v1.11.11.0](https://github.com/multiversx/mx-chain-mainnet-config/releases/tag/v1.11.11.0) (Aug 10) | v2.0.0.0 ([#259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259), open) | unchanged; #259 static three digests running |
| mx-chain-vm-go | [v1.5.47](https://github.com/multiversx/mx-chain-vm-go/releases/tag/v1.5.47) (Jul 30) | — | unchanged; quiet |
| mx-chain-proxy-go | [v1.3.4](https://github.com/multiversx/mx-chain-proxy-go/releases/tag/v1.3.4) (Oct 29) | [v1.5.0](https://github.com/multiversx/mx-chain-proxy-go/releases/tag/v1.5.0) (Aug 10) | unchanged |
| mx-chain-simulator-go | [v1.11.8](https://github.com/multiversx/mx-chain-simulator-go/releases/tag/v1.11.8) (Jul 3) | — | unchanged |
| mx-sdk-rs | [v0.66.2](https://github.com/multiversx/mx-sdk-rs/releases/tag/v0.66.2) (Jun 18) | rc/v0.67 ([#2378](https://github.com/multiversx/mx-sdk-rs/pull/2378), 81 days open) | unchanged tag; #2416 merged into rc/v0.67, #2418 opened |
| mx-sdk-dapp | [v5.7.1](https://github.com/multiversx/mx-sdk-dapp/releases/tag/v5.7.1) (Jul 14) | — | unchanged; quiet |
| mx-sdk-js-core | [v16.0.0](https://github.com/multiversx/mx-sdk-js-core/releases/tag/v16.0.0) (Jul 27) | — | unchanged |
| mx-template-dapp | — (unversioned repo) | — | quiet |
| mx-api-service | [v1.20.0](https://github.com/multiversx/mx-api-service/releases/tag/v1.20.0) (Jun 4) | — | unchanged; #1597 quiet |
| mx-chain-es-indexer-go | [v1.10.2](https://github.com/multiversx/mx-chain-es-indexer-go/releases/tag/v1.10.2) (Apr 3) | [v1.9.3-fix2](https://github.com/multiversx/mx-chain-es-indexer-go/releases/tag/v1.9.3-fix2) (Jul 22) | unchanged |

## Strategic read

1. **The `feat/recon-by-meta` merge is the clearest proof-of-progress signal in weeks — but it's an internal staging move, not a ship.** `feat/testnet-fixes` still has to make it to `master`/a tag before this reaches anyone running a node. No tag accompanied today's merge, so the next digest should watch specifically for a `v2.0.4`-class release as the real completion signal.
2. **`#7951`'s eighth stuck digest, combined with the newly-surfaced gas-limit change inside it, argues for reading PR diffs directly rather than just the target branch's live config.** Yesterday's "unchanged" read was accurate for the target branch but missed that the PR itself carries more than previously reported. Worth making direct-PR-diff checks the default for any long-stalled PR, not just branch-file spot checks.
3. **The `ssd04` nonce/execution-ordering thread is now two-for-two on notifier-go merges across two consecutive days**, while the `mx-chain-go` side of the same author's work (`#7951`) stays stuck — an asymmetry worth a direct question if it persists into a third day.
4. **The host-driver fix cycle (communication-go `#85` → `mx-chain-go` `#7958`) closed in under 24 hours, but `#86` hasn't made the same trip yet** — a small thing to watch, but a good test of whether that pipeline stays fast for the smaller follow-up too.
5. **`sasurobert` remains fully silent** — re-checked today via merged-PR search and public events; nothing beyond unrelated personal-repo activity from January/February. No new information on `GHSA-hwg4-v7gq-phfc`.
6. **mainnet-config `#259` is now at three static digests** — the recommendation from yesterday stands: a direct check-in with the team would resolve this faster than continued daily re-verification.

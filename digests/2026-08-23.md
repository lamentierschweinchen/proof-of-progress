# 2026-08-23 — MultiversX Proof of Progress

**Window:** 2026-08-22 → 2026-08-23 (since last digest)

## TL;DR

Zero merged PRs across the entire 14-repo watchlist since yesterday's digest — the quietest window this series has logged. The one substantive thing that happened: `mx-sdk-dapp`'s still-unopened `rm/transaction-tracking` branch got a real follow-up commit, [`ea62c22` "harden tx tracking for sub-second finality"](https://github.com/multiversx/mx-sdk-dapp/commit/ea62c22) (`radumojic`), which drops the websocket batch-update debounce from 500ms to 150ms and rewrites `trackTransactions.ts` to stop duplicate subscriptions and detect new pending sessions immediately instead of waiting on a websocket status change — a real fix for a race a 10x-faster chain would expose, not just another constant tweak. In parallel, `mx-sdk-dapp-ui` picked up a new `rm/update-storybook` branch continuing yesterday's transaction-toast UI work alongside an unrelated Storybook v10 migration, now at `0.1.25-alpha.4`. Every previously-tracked open item — `mx-chain-go` #7971, #7951, `mainnet-config` #259, `mx-sdk-rs` #2378 — sits with zero new commits. No new releases anywhere, `sasurobert` still silent. Stats: 345 commits / 56 PRs merged in the rolling 28-day window (2026-07-26 → 2026-08-23), `AdoAdoAdo` still the top contributor (121 commits).

## Since the last digest

- **Zero merged PRs across all 14 watchlist repos.** Checked each repo's most-recent-merge timestamp individually: the latest merge anywhere on the watchlist is still `mx-chain-go` [#7972](https://github.com/multiversx/mx-chain-go/pull/7972) from Aug 21. No repo produced a new merge in this window — the first fully-zero-merge digest window this series has tracked.
- **`mx-sdk-dapp`'s `rm/transaction-tracking` branch (still no PR) picked up a substantive hardening commit.** `ea62c22` "harden tx tracking for sub-second finality" (`radumojic`, 14:01:29Z): bumps to `5.7.2-alpha.2` (pins `sdk-dapp-ui` to `>=0.1.25-alpha.2`); drops `MESSAGE_DELAY` (the websocket batch-update debounce in `initializeWebsocketConnection.ts`) from 500ms to 150ms; and rewrites `src/methods/trackTransactions/trackTransactions.ts` — `setupWebSocketTracking` now guards against re-subscribing when already active and unsubscribes stale listeners on re-init, and a new `getPendingSessionIds` diff triggers `recheckStatus()` the instant a new session enters the pending set, rather than waiting for a websocket status transition. Polling now keeps running whenever any session is pending, independent of websocket state. 113 new lines of tests cover the new pending-session and reconnection-dedup paths. This targets a real race: on a chain fast enough, a transaction batch could settle before the old status-change-triggered logic caught up.
- **`mx-sdk-dapp-ui`: a new `rm/update-storybook` branch (built on `rm/style-fixes`) continues the transaction-toast work.** Further "toast UI fixes" and "small UI fixes" commits touch `transaction-toast-progress`, `SignTransactionsOverview` styles, and `Trim` styles — the same UI thread flagged yesterday. Bundled into the same branch: an unrelated Storybook v10 migration ("migrated Storybook from `@storybook/html-vite` renderer to `@stencil/storybook-plugin`", "removed proxies build flow"). `package.json` now at `0.1.25-alpha.4`. Still no PR on either `sdk-dapp` or `sdk-dapp-ui`.
- **All four previously-tracked open items: unchanged.** [`mx-chain-go` #7971](https://github.com/multiversx/mx-chain-go/pull/7971) — no commits since Aug 21. [`#7951`](https://github.com/multiversx/mx-chain-go/pull/7951) "extra txs api logs" — seventeenth straight digest with no movement, no real commit since Aug 10. [`mx-chain-mainnet-config` #259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259) — no commits since Aug 20; `raduchis`'s Aug 21 review flags (bandwidth comment, a regressed value, two projected-activation timestamps) still unaddressed. [`mx-sdk-rs` #2378](https://github.com/multiversx/mx-sdk-rs/pull/2378) — unchanged since Aug 13.
- **No new releases across the watchlist.** Latest tags everywhere are still from Aug 19.
- **`sasurobert`: still no activity in the org.** Merged-PR search unchanged since early February 2026 (personal-repo work only); public events feed empty.
- **Org-wide sweep: only `mx-sdk-dapp` and `mx-sdk-dapp-ui` show pushes since the last digest** — both covered above. No other org repo, on or off watchlist, pushed in this window. No new advisory-flagged branches.

## Themes

### `mx-sdk-dapp` keeps hardening its Supernova-clock tracking, three days pre-PR

The `rm/transaction-tracking` branch has now gone from a constants-only tweak (Aug 21) to a real subscription-lifecycle fix (today): duplicate-subscription guards, immediate pending-session detection, and a tighter websocket debounce. This is the strongest evidence yet that the change is heading toward a real fix rather than a quick alpha bump — but it's still sitting on a branch with no CHANGELOG-referenced `#1737` PR opened three days in.

### The rest of the watchlist went fully quiet

No merges anywhere else on the 14-repo watchlist for the first time this series has tracked. Not itself alarming — the repos that were already stalled (`mainnet-config`, `#7951`, `mx-sdk-rs` rc) were already stalled before this window — but worth flagging if a second consecutive zero-merge day follows.

### Stuck items unchanged

`#7951` (seventeenth digest, no real commits since Aug 10) and `mx-sdk-rs` `rc/v0.67` `#2378` (unchanged since Aug 13) both sit exactly where they were.

## Version map

| Component | Stable | Pre-release | Δ |
|---|---|---|---|
| mx-chain-go | [v1.11.11](https://github.com/multiversx/mx-chain-go/releases/tag/v1.11.11) (Aug 10) | [v2.0.4](https://github.com/multiversx/mx-chain-go/releases/tag/v2.0.4) (Aug 19) | unchanged |
| mx-chain-mainnet-config | [v1.11.11.0](https://github.com/multiversx/mx-chain-mainnet-config/releases/tag/v1.11.11.0) (Aug 10) | v2.0.0.0 ([#259](https://github.com/multiversx/mx-chain-mainnet-config/pull/259), open) | unchanged; #259 still no new commits since Aug 20 |
| mx-chain-vm-go | [v1.5.47](https://github.com/multiversx/mx-chain-vm-go/releases/tag/v1.5.47) (Jul 30) | [v1.5.48](https://github.com/multiversx/mx-chain-vm-go/releases/tag/v1.5.48) (Aug 19) | unchanged |
| mx-chain-proxy-go | — (all pre-release) | [v1.5.1](https://github.com/multiversx/mx-chain-proxy-go/releases/tag/v1.5.1) (Aug 19) | unchanged |
| mx-chain-simulator-go | [v1.11.8](https://github.com/multiversx/mx-chain-simulator-go/releases/tag/v1.11.8) (Jul 3) | — | unchanged; quiet |
| mx-sdk-rs | [v0.66.2](https://github.com/multiversx/mx-sdk-rs/releases/tag/v0.66.2) (Jun 18) | rc/v0.67 ([#2378](https://github.com/multiversx/mx-sdk-rs/pull/2378), open, unchanged since Aug 13) | unchanged |
| mx-sdk-dapp | [v5.7.1](https://github.com/multiversx/mx-sdk-dapp/releases/tag/v5.7.1) (Jul 14) | — (unreleased `5.7.2-alpha.2` on `rm/transaction-tracking`, no PR) | branch work continues, no tag |
| mx-sdk-js-core | [v16.0.0](https://github.com/multiversx/mx-sdk-js-core/releases/tag/v16.0.0) (Jul 27) | — | unchanged |
| mx-template-dapp | — (unversioned repo) | — | quiet |
| mx-api-service | [v1.21.0](https://github.com/multiversx/mx-api-service/releases/tag/v1.21.0) (Aug 19) | — | unchanged |
| mx-chain-es-indexer-go | — (all pre-release) | [v1.10.3](https://github.com/multiversx/mx-chain-es-indexer-go/releases/tag/v1.10.3) (Aug 19) | unchanged |

## Strategic read

1. **First fully zero-merge day this series has tracked across the whole watchlist.** Not itself a red flag — the repos most active recently were already between merges — but a second consecutive quiet day would be worth naming explicitly as a cadence break.
2. **The `mx-sdk-dapp` tx-tracking work is maturing from constant tuning into a real concurrency fix.** The subscription-dedup and immediate pending-session detection address an actual bug class a 10x round-time drop exposes. Three days of branch work with no PR opened yet is the thing to watch — flag whether review catches anything in the rewritten subscribe/unsubscribe logic once it does.
3. **`mainnet-config` #259's review flags are now unaddressed for a second full day.** Still not urgent on its own, but this is exactly the kind of correctness issue that shouldn't linger close to a mainnet cutover — worth a direct check-in per the standing recommendation.
4. **`#7951` is now the longest-stuck single item this series has tracked — seventeen digests, thirteen days since its last real commit.** Standing recommendation unchanged: a direct check-in would resolve it faster than continued daily re-verification.
5. **`sasurobert` remains fully silent**, unchanged for well over a month aside from the already-covered July `GHSA-hwg4-v7gq-phfc` work.
